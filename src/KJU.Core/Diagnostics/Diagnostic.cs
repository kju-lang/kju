namespace KJU.Core.Diagnostics
{
    public class Diagnostic
    {
        /*
            status: Warning/Error
            type: "Magic string" <- every component defines for itself.
            message: String <- format "Error is here {0} and here {1} and here {2}"
            ranges: IReadOnlyList<Range>
         */
    }
}